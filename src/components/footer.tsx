import styles from './styles/footer.module.css';

const Footer = () => {
  return (
    <footer>
      <p className={styles.footer}>出典：RESAS（地域経済分析システム）</p>
    </footer>
  );
};

export default Footer;
